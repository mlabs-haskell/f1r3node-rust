new executePetta(`rho:petta:execute`), stdout(`rho:io:stdout`), retCh in {
  executePetta!("(p 1 2) (p x y) (= (swap (Pair $x $y)) (Pair $y $x)) (= (main $_) (match &self (p $a $b) (swap (Pair $a $b))))", *retCh) |
  for(@ok <- retCh) {
      stdout!(ok)
  }
}
